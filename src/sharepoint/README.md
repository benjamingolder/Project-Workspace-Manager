# SharePoint

Enthält SharePoint-Konfigurationen, Site-Templates und PowerShell-Skripte.

## Unterordner
- `templates/` – SharePoint Site Templates (.xml)
- `scripts/` – PowerShell-Skripte für Konfiguration und Deployment
- `listen/` – SharePoint Listen-Schemas (JSON)
